FROM ghcr.io/openclaw/openclaw:latest

ENV gateway.mode=local

CMD ["sh", "-c", "mkdir -p /data/.openclaw && echo
'{\"gateway\":{\"mode\":\"local\",\"controlUi\":{\"dangerouslyDisableDeviceAuth\":true}}}' >
/data/.openclaw/openclaw.json && node dist/index.js gateway --port $PORT --bind lan"]
